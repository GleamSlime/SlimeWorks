output: dist/
releases:
  - name: release
    jobs:
      - name: release-macos
        package:
          platform: macos
          target: dmg
          build_args:
            release: true
      - name: release-windows
        package:
          platform: windows
          target: exe
          build_args:
            release: true
