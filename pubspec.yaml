name: slime_works
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+17

environment:
  sdk: ^3.11.0-87.0.dev

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  # cupertino_icons: ^1.0.8
  rust_lib_slime_works:
    path: rust_builder
  flutter_rust_bridge: 2.11.1
  window_manager: ^0.5.1 # 窗口管理
  tray_manager: ^0.5.0 # 系统托盘
  flutter_dotenv: ^6.0.0 # DOTENV配置
  flustars: ^2.0.1 # Dart常用工具类库

  # 状态管理
  get: ^4.7.3 # 轻量级状态管理和路由库
  get_it: ^9.2.0 # 依赖注入容器

  # 路由管理
  go_router: ^17.0.1 # 声明式路由管理

  # 网络请求
  dio: ^5.9.0 # HTTP客户端
  http: ^1.2.0 # HTTP客户端
  cupertino_http: ^2.4.0 # iOS Debug 下使用 URLSession 绕过 socket 栈问题
  crypto: ^3.0.7 # PicACG Dart 调试回退签名
  uuid: ^4.5.3 # PicACG Dart 调试回退 nonce
  connectivity_plus: ^6.0.5 # 网络状态监听

  # 本地存储
  flutter_secure_storage: ^10.0.0 # 安全存储敏感数据
  shared_preferences: ^2.3.4 # 简单键值对存储
  path_provider: ^2.1.5 # 获取应用路径
  file_picker: ^10.3.10 # 文件选择器

  # 图片相关
  cached_network_image: ^3.4.1 # 网络图片缓存
  flutter_svg: ^2.2.3 # SVG图片支持
  desktop_drop: ^0.5.0 # 桌面端拖拽文件支持
  video_player: ^2.10.1 # 图片/视频预览（Android/iOS/macOS/Web）
  media_kit: ^1.1.11 # 跨平台视频播放（Windows/Linux + 全平台）
  media_kit_video: ^1.2.4 # media_kit 视频渲染
  media_kit_libs_windows_video: ^1.0.9 # Windows 平台 FFmpeg 解码支持
  media_kit_libs_macos_video: ^1.0.9 # macOS 平台 MPV 解码支持
  media_kit_libs_ios_video: ^1.0.9 # iOS 平台 MPV 解码支持
  gal: ^2.3.0 # 保存图片/视频到系统相册（iOS/Android/macOS）
  image: ^4.1.3 # 纯 Dart 图片解码/缩放/编码（服务端高性能封面缩略图生成）

  # UI组件
  flutter_easyloading: ^3.0.5 # 简单的加载提示
  easy_refresh: ^3.4.0 # 下拉刷新上拉加载
  flutter_slidable: ^4.0.3 # 滑动操作组件
  skeletonizer: ^2.1.2 # 骨架屏加载效果
  flutter_widget_from_html: ^0.15.2 # HTML渲染器（含图片/链接等完整支持）
  html: ^0.15.6 # HTML 解析库

  # 屏幕适配
  flutter_screenutil: ^5.9.3 # 屏幕适配方案

  # 设备信息
  device_info_plus: ^12.3.0 # 获取设备信息
  package_info_plus: ^8.0.0 # 获取应用版本信息
  permission_handler: ^12.0.1 # 权限管理
  share_plus: ^12.0.2
  cue: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4

  # 代码生成工具
  build_runner: ^2.10.5 # 代码生成运行器
  flutter_gen_runner: ^5.12.0 # FlutterGen资源生成器
  json_serializable: ^6.12.0 # JSON序列化生成器
  go_router_builder: ^4.1.3 # GoRouter路由生成器

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/colors.xml
    - assets/image/
    - assets/image/svg/
    - assets/image/tray/

  # Shaders for custom effects
  shaders:
    - shaders/gooey.frag

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  fonts:
    - family: FZLanTingYuanS-EB-GB
      fonts:
        - asset: assets/fonts/FZLanTingYuanS-R-GB.ttf
          weight: 300
        - asset: assets/fonts/FZLanTingYuanS-EB-GB.ttf
          weight: 400
        - asset: assets/fonts/FZLanTingYuanS-EB-GB.ttf
          weight: 500
        - asset: assets/fonts/FZLanTingYuanS-EB-GB.ttf
          weight: 600
        - asset: assets/fonts/FZLanTingYuanS-EB-GB.ttf
          weight: 700

# FlutterGen configuration
# dart run build_runner build --delete-conflicting-outputs
flutter_gen:
  output: lib/gen/ # 生成的代码输出目录
  line_length: 120 # 代码行长度

  # 颜色资源配置
  colors:
    inputs:
      - assets/colors.xml

  # 资源文件配置
  assets:
    enabled: true # 启用资源生成
    outputs:
      package_parameter_enabled: false
      style: dot-delimiter # 使用点分隔符风格
      class_name: Assets
